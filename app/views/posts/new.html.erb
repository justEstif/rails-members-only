<header>
  <h1>New Post</h1>
</header>
<%= form_with model: @post do |form| %>
  <fieldset>
    <%= form.label :title %>
    <%= form.text_field :title %>
    <%= form.label :body %>
    <%= form.text_area :body%>
  </fieldset>
  <%= form.submit %>
<% end%>
